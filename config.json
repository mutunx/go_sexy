{
	"root":"www.mmjpg.com",
	"regex":{
		"image":[ ##匹配页面上的图片地址
			{
				"exp":"src=\"([^\"?]+)", ##匹配图片的正则表达式
				"match":1, ##指定图片地址再正则表达式里的哪个分组，0表示整个表达式匹配的内容，1表示第一个分组
				"folder":"none" ##存放图片的文件夹，可选值url,title,none,正则表达式,文件夹名称
			}
		],
		"page":[], ##正则表达式，只有符合的页面才会被抓取并解析，空白表示所有页面都抓取
		"imgInPage":["\S+/mm/\S+"], ##存放正则，指定图片存在于哪些页面
		"href":[ ##匹配页面上的链接
			{
				"exp":"\s+href=\"([a-zA-Z0-9_\-/:\.%?=]+)\"", ##存放匹配链接的正则表达式
				"match":1 ##指定分组
			}
		]
    }
}
